version: "3.9"

services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - db_data:/app/data
    env_file:
      - .env.production
    restart: unless-stopped

volumes:
  db_data:
